import "./course.css";

function DifficultyLevel({ difficulty }) {
    return <></>;
}

export default function Course({ id, title, category, difficulty, thumbnail, instructor: { name, pic }, rating }) {
    return <div>Course</div>;
}
